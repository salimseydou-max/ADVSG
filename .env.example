## Expo client (public)
# For native builds, provide a full URL to the translation function (Vercel).
# For web, you can omit this and `/api/translate` will be used on the same domain.
EXPO_PUBLIC_TRANSLATE_ENDPOINT=https://your-vercel-domain.vercel.app/api/translate

## App identifiers (used by app.config.ts; NOT exposed to the client)
IOS_BUNDLE_IDENTIFIER=com.yourcompany.advice
IOS_BUILD_NUMBER=1
ANDROID_PACKAGE=com.yourcompany.advice

## Vercel serverless translate endpoint (server-side only)
LIBRETRANSLATE_URL=https://libretranslate.de/translate
# LIBRETRANSLATE_API_KEY=your_key_if_needed

